name: Find My Files
on: [push]
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: List all files in repository
      run: |
        echo "--- ROOT DIRECTORY ---"
        ls -la
        echo "--- ONE LEVEL DEEP ---"
        ls -R
        
